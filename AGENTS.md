# Persistent Rules & Instructions

- **Version & Release Bumping**: Automatically increment `versionCode` and update `versionName` (e.g. `1.0.0-beta.2` -> `1.0.0-beta.3`, etc.) in `app/build.gradle.kts` after making any codebase changes.
